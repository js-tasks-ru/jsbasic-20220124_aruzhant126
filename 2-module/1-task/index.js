function sumSalary(salaries) {
  let salaries = {
    John: 1000,
    Ann: 1600,
    Pete: 1300,
    month: 'December',
    currency: 'USD',
    isPayed: false
  }
  if (Object.values(salaries) == 'number') {
    let totalSum = 0
      totalSum += Object.values(salaries);
  }
  return 0
}
      
sumSalary(salaries);
